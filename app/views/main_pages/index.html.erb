
<main class="form-signin">
<div class="container-fluid center">
    <p class="reloj"> 18:00:00 </p>
</div>
  <h3 class="h3 mb-3">Porfavor ingresa tus credenciales.</h3>
  <%= bootstrap_form_for(@attendance) do |f| %>
  <div class="form-floating">
    <%= f.text_field :employee_id, class: "form-control", placeholder: "ID de trabajador", hide_label: true %>
    <%= f.password_field :employee_id, class: "form-control", placeholder: "Numero Privado", hide_label: true %>
  <div class="form-floating">
    <%= f.submit "Check", class: "w-100 btn btn-lg btn-primary" %>
  <% end %>
</main>