<main class="form-signin text-center">
  <i class="fa-solid fa-store fa-9x"></i>
    <%= form_with(model: store) do |form| %>
      <div class="form-floating">

        <div class="mb-3 mt-5 form-group">
          <%= form.text_field :name, class: "form-control", placeholder: "Nombre de la tienda"%>
        </div>

        <div class="mb-3 form-group">
          <%= form.text_field :address, class: "form-control", placeholder: "Dirección" %>
        </div>

        <div class="actions">
          <%= form.submit class: "btn btn-success btn-lg w-100"%>
        </div>

        <div class="mt-3">
          <%= link_to "Back", stores_path,class: "btn btn-dark btn-lg w-100"%>
        </div>

      </div>
    <% end %>
</main>